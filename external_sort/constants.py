class Arguments:
    """Класс с аргументами командной строки."""

    FILES = "--f"
    KEY = "--k"
    OUTPUT_FILE = "--o"
    REVERSE = "--r"
    TYPE_DATA = "--t"


class Actions:
    """Класс с действиями."""

    EXIT = "exit"
    ADD_SETTINGS = "add_settings"
    SORT = "sort"
